use flake

# Automatically target this worktree's emulator instance if present.
if [ -f .emulator-serial ]; then
	serial="$(tr -d '\r\n' < .emulator-serial)"
	if [ -n "$serial" ]; then
		export ANDROID_SERIAL="$serial"
	fi
fi
